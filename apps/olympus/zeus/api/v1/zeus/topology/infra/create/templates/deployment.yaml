apiVersion: apps/v1
kind: Deployment
metadata:
  name: "zeus-client"
  labels:
    app.kubernetes.io/instance: "zeus-client"
    app.kubernetes.io/name: "zeus-client"
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: "zeus-client"
      app.kubernetes.io/instance: "zeus-client"
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "zeus-client"
        app.kubernetes.io/instance: "zeus-client"
    spec:
      containers:
          resources:
            {}
      volumes:
      nodeSelector:
        {}
      affinity:
        {}
      tolerations:
        []
