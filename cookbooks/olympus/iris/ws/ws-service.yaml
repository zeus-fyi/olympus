apiVersion: v1
kind: Service
metadata:
  name: "iris-ws"
  labels:
    app.kubernetes.io/instance: "iris"
    app.kubernetes.io/name: "iris"
    app.kubernetes.io/managed-by: "zeus"
spec:
  type: ClusterIP
  ports:
    - port: 8008
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/instance: "iris"
    app.kubernetes.io/name: "iris"

